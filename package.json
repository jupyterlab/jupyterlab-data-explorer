{
  "private": true,
  "workspaces": [
    "dataregistry-core",
    "dataregistry",
    "dataregistry-extension",
    "jupyterlab/packages/*"
  ],
  "scripts": {
    "build": "tsc --build dataregistry-core dataregistry dataregistry-extension",
    "build:watch": "yarn run build --watch"
  }
}
